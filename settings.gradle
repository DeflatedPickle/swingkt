pluginManagement {
    plugins {
        id 'org.jetbrains.kotlin.jvm' version "$kotlinVersion"

        id 'com.diffplug.spotless' version "$spotlessVersion"
    }
}

rootProject.name = 'swingkt'

include 'master'
include 'demo'
